import "./../../components/css-grid-editor/css-grid-editor.js"

export default class CssGridViewModel extends crsbinding.classes.ViewBase {
    async connectedCallback() {
        await super.connectedCallback();
    }
}